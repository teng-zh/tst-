# SSH客户端使用指南

本文介绍几款常用的SSH客户端，帮助你连接到云服务器。

## 一、推荐SSH客户端

### 1. Xshell（推荐）
- **特点**：功能强大，界面友好，支持多标签，有免费版
- **适用系统**：Windows
- **下载地址**：https://www.xshell.com/zh/free-for-home-school/

### 2. FinalShell
- **特点**：国产SSH客户端，功能丰富，支持文件管理，免费
- **适用系统**：Windows、Mac、Linux
- **下载地址**：http://www.hostbuf.com/

### 3. Bitvise SSH Client
- **特点**：安全可靠，支持SFTP，免费使用
- **适用系统**：Windows
- **下载地址**：https://www.bitvise.com/ssh-client-download

### 4. Windows Terminal（内置）
- **特点**：Windows 10/11内置，支持SSH，简洁高效
- **适用系统**：Windows 10/11
- **开启方法**：设置 -> 应用 -> 可选功能 -> 添加功能 -> 搜索"OpenSSH客户端"

## 二、Xshell使用步骤

### 1. 下载安装
- 访问上述地址下载免费版Xshell
- 按照提示安装，无需特殊配置

### 2. 创建连接
1. 打开Xshell，点击左上角的"新建"按钮
2. 在"新建会话属性"窗口中配置：
   - **名称**：任意，如"文件分享服务器"
   - **主机**：你的云服务器**公网IP地址**
   - **端口号**：22
   - **协议**：SSH
3. 点击"确定"保存

### 3. 配置密钥认证
1. 选中刚创建的连接，点击"属性"
2. 在左侧导航中选择 **连接 > 用户身份验证**
3. 选择"Public Key"认证方式
4. 点击"浏览" -> "用户密钥管理者"
5. 点击"导入"，选择你的私钥文件（如 `file-share-key.pem`）
6. 点击"关闭"，返回属性窗口
7. 用户名填写 `root`
8. 点击"确定"保存配置

### 4. 连接服务器
1. 在会话管理器中双击创建的连接
2. 首次连接会提示"SSH安全警告"，点击"接受并保存"
3. 成功连接后，会显示服务器的命令行界面

## 三、FinalShell使用步骤

### 1. 下载安装
- 访问上述地址下载对应系统的FinalShell
- 按照提示安装

### 2. 创建连接
1. 打开FinalShell，点击左上角的"新建连接"按钮
2. 在"新建连接"窗口中选择"SSH连接"
3. 填写配置信息：
   - **名称**：任意，如"文件分享服务器"
   - **主机**：你的云服务器**公网IP地址**
   - **端口**：22
   - **用户名**：root
   - **认证方式**：选择"公钥"
   - **私钥路径**：选择你的私钥文件（如 `file-share-key.pem`）
4. 点击"确定"保存

### 3. 连接服务器
1. 在左侧导航中双击创建的连接
2. 首次连接会提示"是否信任此主机"，点击"是"
3. 成功连接后，会显示服务器的命令行界面和文件管理界面

## 四、Windows Terminal使用步骤

### 1. 配置SSH
1. 确保已安装OpenSSH客户端（Windows 10/11默认已安装）
2. 打开Windows Terminal
3. 点击顶部的"+" -> "设置"
4. 在左侧导航中选择"添加新配置文件" -> "新建空配置文件"
5. 填写配置信息：
   - **名称**：任意，如"文件分享服务器"
   - **命令行**：`ssh -i "C:\path\to\your\file-share-key.pem" root@你的公网IP`
   - **启动目录**：任意
6. 点击"保存"

### 2. 连接服务器
1. 在Windows Terminal中选择创建的配置文件
2. 首次连接会提示"Are you sure you want to continue connecting?"，输入"yes"并回车
3. 成功连接后，会显示服务器的命令行界面

## 五、注意事项

### 1. 密钥文件安全
- 私钥文件是连接服务器的重要凭证，必须妥善保管
- 不要将私钥文件分享给他人
- 建议备份私钥文件到多个安全位置

### 2. 服务器安全
- 连接成功后，建议立即修改root密码：
  ```bash
  passwd root
  ```
- 配置安全组，只开放必要的端口（22、80、443）
- 定期更新系统和软件：
  ```bash
  apt update && apt upgrade -y
  ```

### 3. 连接问题排查
- 检查云服务器的**公网IP地址**是否正确
- 检查云服务器的**安全组**是否开放了22端口
- 检查私钥文件是否正确，权限是否设置正确
- 检查网络连接是否正常

### 4. 常见错误

| 错误信息 | 可能原因 | 解决方案 |
|----------|----------|----------|
| Permission denied (publickey) | 密钥文件错误或权限问题 | 检查密钥文件路径和权限，确保使用正确的私钥 |
| Connection refused | 服务器未启动SSH服务或端口未开放 | 检查SSH服务状态，检查安全组规则 |
| Connection timed out | 网络连接问题或服务器未运行 | 检查网络连接，检查服务器状态 |

## 六、后续操作

连接成功后，你可以开始部署文件分享站：

1. 更新系统：
   ```bash
   apt update && apt upgrade -y
   ```

2. 安装Python和依赖：
   ```bash
   apt install python3 python3-pip python3-venv -y
   ```

3. 按照部署指南继续操作

## 七、联系方式

如果遇到连接问题，可以参考：
- 阿里云ECS文档：https://help.aliyun.com/document_detail/25434.html
- 或联系阿里云技术支持

祝你连接成功！